package bi.leo.picker.common.text.processor;

import bi.leo.picker.common.text.Content;

public class DefaultContentProcessor implements IContentProcessor{
    @Override
    public Content process(Content content) {
        return content;
    }
}
